spring.application.name=system-pqrs

# MongoDB — sobreescribe MONGODB_URI en produccion (Atlas)
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://santimarimon66:Mongo123456@jacpolicarpa.kfl7ht6.mongodb.net/JACDB?retryWrites=true&w=majority}
spring.data.mongodb.database=${MONGODB_DATABASE:JACDB}

# Server
server.port=8080

# JWT — NUNCA hardcodear en produccion, usar variable de entorno JWT_SECRET
jwt.secret=${JWT_SECRET:MiClaveSecretaSuperSeguraParaJWTDe256BitsOmas2026}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Multipart — subida de evidencias
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Uploads — sobreescribir en produccion con ruta absoluta
app.upload.dir=${UPLOAD_DIR:./uploads/}
